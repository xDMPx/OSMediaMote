# OSMediaMote Android

## Building

The application is not currently distributed. To build and run it, clone the repository, open the project in Android Studio, and build and install it on an Android device or emulator.

